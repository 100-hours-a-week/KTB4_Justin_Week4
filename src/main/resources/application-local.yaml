spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/tunelog?serverTimezone=Asia/Seoul&characterEncoding=UTF-8}
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${DB_USERNAME:tunelog}
    password: ${DB_PASSWORD:tunelog-local}
